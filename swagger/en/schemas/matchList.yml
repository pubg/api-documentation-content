title: Match List
description: "Contains a list of match IDs"
type: object
properties:
  data:
    type: array
    items:
      type: object
      properties:
        type:
          type: string
          description: Identifier for this object type ("match")
        id:
          type: string
          description: "Match ID - Used to lookup the full match object on the /matches endpoint"

