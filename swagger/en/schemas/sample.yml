title: Sample Object
description: "Sample objects contain the ID of a match."
type: object
properties:
  type:
    type: string
    description: Identifier for this object type ("match")
  id:
    type: string
    description: "Match ID - Used to lookup the full match object on the /matches endpoint"
