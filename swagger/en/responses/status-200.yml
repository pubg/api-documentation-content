description: Status OK
content:
  application/vnd.api+json:
    schema:
      type: object
      properties:
        id:
          type: string
        attributes:
          type: object
          properties:
            releasedAt:
              type: string
              format: dateTime
              description: "The most recent release date"
            version:
              type: string
              description: "The version of the API service"
