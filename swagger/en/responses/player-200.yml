description: Player response
content:
  application/vnd.api+json:
    schema:
      type: object
      properties:
        data:
          $ref: '../schemas/index.yml#/player'
        links:
          type: object
          description: "Pagination links"
          properties:
            self:
              type: string
              description: "Link to this player object"
        meta:
          type: object
          description: "N/A"
