accountId:
  $ref: './accountId.yml'

filterCreatedAt-start:
  $ref: './filterCreatedAt-start.yml'

filterPlayerIds:
  $ref: './filterPlayerIds.yml'

filterPlayerNames:
  $ref: './filterPlayerNames.yml'

gameMode:
  $ref: './gameMode.yml'

id:
  $ref: './id.yml'

seasonId:
  $ref: './seasonId.yml'
