id:
  $ref: './id.yml'

accountId:
  $ref: './accountId.yml'

seasonId:
  $ref: './seasonId.yml'

filterPlayerIds:
  $ref: './filterPlayerIds.yml'

filterPlayerNames:
  $ref: './filterPlayerNames.yml'

filterCreatedAt-start:
  $ref: './filterCreatedAt-start.yml'
