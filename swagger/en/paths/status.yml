get:
  tags:
  - Status
  summary: Check the status of the API.
  description: "The status endpoint can be called to verify that the API is up and running. It also provides the most recent release date and version of the API service itself."
  responses:
    200:
      $ref: '../responses/index.yml#/status-200'
